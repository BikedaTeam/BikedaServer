<nav class="main-header navbar navbar-expand navbar-white navbar-light">
  <!-- Left navbar links -->
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
    </li>
  </ul>

  <!-- Right navbar links -->
  <ul class="navbar-nav ml-auto">
    <li class="nav-item">
      <a href="#" class="nav-link">
        <span class="d-none d-md-inline">
          <%= branch.adminNm  %>(
          <% if(branch.adminCelno.length==11) { %>
              <%= branch.adminCelno.replace(/(\d{3})(\d{4})(\d{4})/, '$1-$2-$3') %>
          <% } else if(branch.adminCelno.length==8){ %>
              <%= branch.adminCelno.replace(/(\d{4})(\d{4})/, '$1-$2') %>
          <% } else { %>
            <% if(branch.adminCelno.indexOf('02')==0){ %>
                <%= branch.adminCelno.replace(/(\d{2})(\d{4})(\d{4})/, '$1-$2-$3') %>
            <% } else { %>
                <%= branch.adminCelno.replace(/(\d{3})(\d{3})(\d{4})/, '$1-$2-$3') %>
            <% } %>
          <% } %>
          )
        </span>
      </a>
    <li>
    <li class="nav-item">
      <a id="btn_logout" href="#" class="nav-link">
        <i class="nav-icon fas fa-sign-out-alt"></i>로그아웃
      </a>
    </li>
  </ul>
</nav>
